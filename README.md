# Maze Game Project
My 12th grade cyber project.
This is a browser game written in TypeScript on React (backend in python), which includes mazes.
The core idea of the game is to 
The work on this game is still in progress!


## Local Deploy
### Deploying Website
Inside the frontend folder, first use `npm install` to install dependencies.
Then, to deploy the website (on vite) run:
```
npm run dev
```

### Deploying Server
Inside the `backend` folder, make sure you have installed the dependencies (TODO) and run:
```
python server.py
```
This will launch the server on localhost.
(TODO: Add instructions for managing deployment configuration)
